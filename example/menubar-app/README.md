# macOS example

A SwiftUI menu bar app driving the Go worker in `../worker`.

```bash
./build.sh
./.build/debug/MenuBarExample
```

Look for the icon in the menu bar.

To add vero to a macOS app of your own, starting from the Go worker: the
[README](../../README.md#create-a-vero-macos-app-on-your-mac).
